@using CSharpFunctionalExtensions;

@model Result
@{
    ViewBag.Title = "ConfirmEmail";
}

<h2>ConfirmEmail</h2>
@if (ViewBag.Error != null)
{
    @ViewBag.Error;
}

<p>
    @if (Model.IsFailure)
    {
        @Model.Error
    }

</p>

